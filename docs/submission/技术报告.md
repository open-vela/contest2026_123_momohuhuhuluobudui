# AgentGuard 技术报告

> 2026 首届 openvela AI 硬件开发者大赛 · AI 硬件产品创新赛道

## 1. 作品基本信息

| 项目 | 内容 |
| --- | --- |
| 作品名称 | AgentGuard：基于 openvela 的桌面健康与隐私守护终端 |
| 队伍名称 | momohuhuhuluobudui（仓库登记名） |
| 团队分工 | yhx06：产品设计、openvela/ESP-DL 适配、电脑端联动、实机测试与材料整理 |
| 参赛方向 | AI 硬件产品创新 |
| 代码仓库 | contest2026_123_momohuhuhuluobudui |

## 2. 作品摘要（300 字以内）

AgentGuard 是运行于 ESP32-S3-EYE 和 openvela/NuttX 的桌面健康与隐私守护终端。系统使用 ESP-DL 在设备本地检测并框选人脸，依据人数和持续在场时间触发久坐提醒；隐私模式下出现第二人时，开发板以专用灯效告警并通过 USB 通知电脑显示不透明遮罩。用户可用 BOOT 键确认提醒、切换隐私及演示模式。摄像头画面不上传，事件以有界 JSONL 保存在开发板和电脑端，并可统计久坐、确认及隐私触发次数。系统针对摄像头、推理及电脑断连设计安全降级，兼顾可演示性、隐私与可靠性。

## 3.1 项目背景、挑战与创新

长时间伏案会带来健康风险，而开放办公或居家环境中，屏幕内容又可能被身后来人看到。普通摄像头方案常依赖云端，既增加延迟，也引入图像上传风险。AgentGuard 将两类问题合并为一个端侧闭环：板端只输出人数、框和事件，不向电脑传输摄像头画面。

主要挑战包括：ESP32-S3 上有限内存与算力、ESP-DL 到 NuttX/openvela 的兼容、实时预览与推理共存、USB 断连不能拖死相机循环，以及物理按键在当前板卡上的可用性差异。创新点是将本地人数感知、久坐策略、自动隐私遮罩、双端审计日志和故障安全状态整合到一个小型终端中。

## 3.2 总体架构、选型与关键模块

硬件采用 ESP32-S3-EYE，使用板载摄像头、240×240 LCD、RGB 提示灯和 BOOT 键。操作系统为 openvela/NuttX，视觉推理采用 ESP-DL。电脑端采用仅依赖 Python 标准库的安全代理，以 USB 串口作为演示首选通道，HTTP 作为可选通道，并提供每 2 秒刷新的本地事件统计面板和 CSV 导出。

系统框图：

```text
摄像头 → V4L2/RGB565 → ESP-DL 人脸框与人数 → 健康/隐私状态机
                                               ├→ LCD 检测框与状态
BOOT 键 ────────────────────────────────────────┤→ RGB 差异化灯效
                                               ├→ LittleFS JSONL
                                               └→ USB/HTTP → 电脑通知/遮罩 → JSONL 统计
```

方案对比：云端视觉具有更大模型能力，但会上传画面且依赖网络；颜色启发式资源低但不能可靠识别人脸；ESP-DL 端侧检测在隐私、可复现性和开发周期之间更均衡。USB 比 Wi-Fi 更适合现场演示，免配网且断线影响范围小；HTTP 作为局域网扩展入口保留，并使用 Bearer Token 和事件白名单。

数据流为：V4L2 获取 RGB565 帧 → ESP-DL 检测并产生边界框 → 策略状态机根据人数、时间和模式生成事件 → LCD/RGB/LittleFS 本地反馈 → USB/HTTP 将允许的事件发送至电脑 → 桌面通知或隐私遮罩 → JSONL 留存与统计。

代码按层拆分：

- `core.c`：无硬件依赖的确定性策略状态机。
- `vision_espdl.cpp`：ESP-DL 到 NuttX/openvela 的薄兼容层。
- `agentguard_main.c`：相机、LCD、按键、LED、存储与通信适配。
- `storage.c`：有界 JSONL 保存、裁剪及板端统计。
- `monitor/`：电脑端事件校验、动作派发、日志与统计。

## 3.3 AI 算法、关键机制与 openvela 能力

系统使用 ESP-DL 人脸检测模型输出边界框及人数。项目尽量保留官方算法层，仅适配输入图像、内存和运行接口，以降低移植风险。检测结果不会用于身份识别，也不声称能识别“主人”。

策略层将 AI 输出转化为产品行为：无人暂停久坐计时；单人持续在场累计时间；达到阈值生成 `sedentary_alert`；用户短按 BOOT 生成 `acknowledged`；隐私模式中人数达到 2 时生成 `blur_screen`。事件通过白名单发送，通信使用尽力而为的非阻塞路径，电脑断线不阻塞推理。

openvela/NuttX 提供 V4L2 摄像头、LCD、任务/同步、串口、Flash 文件系统和 NSH 命令环境。板端事件保存在 LittleFS，电脑端代理负责系统通知与遮罩，形成端云无关的本地协同。

模型输出经过 1.5 秒存在性保持以降低单帧漏检造成的界面抖动；隐私动作以状态跃迁触发，避免每帧重复通知。电脑通信路径为非阻塞、尽力而为，失败只丢失本次远端动作，不反向阻塞摄像头和本地告警。模型异常会进入明确的 AI ERROR 安全状态，不使用不可靠的颜色结果冒充 AI 检测。

## 3.4 实现过程、软硬件与交互

开发首先完成摄像头和 LCD 通路，再以 ESP-DL 替换早期颜色启发式检测。随后加入多框映射、人数策略、久坐计时、隐私状态、差异化灯效、LittleFS 留存和 USB 电脑代理。每次功能修改通过主机单测、完整固件编译和实机观察逐层验证。

关键流程：

```text
取帧 → 推理是否成功？ ─否→ AI ERROR 安全状态并继续重试
  │是
  ↓
更新人脸框/人数 → 更新久坐与隐私状态 → 生成去重事件
  ↓                                      ↓
LCD + RGB 本地反馈              LittleFS + USB/HTTP
                                         ↓
                              电脑通知/遮罩 + JSONL 统计
```

关键 BOM：ESP32-S3-EYE 开发板 1 块、USB 数据线 1 根、运行 Linux/Windows/macOS 的电脑 1 台；无需外接传感器。使用板载 OV2640 摄像头、ST7789 LCD、RGB LED 和 BOOT 键。本项目没有开发全新硬件平台或新增板级驱动，主要工作是 openvela 应用与 ESP-DL 兼容适配。

最终交互为：短按 BOOT 确认提醒；按住 3–7 秒后松开切换隐私；按住至少 8 秒后松开切换 Demo。Demo 将久坐阈值缩短至 20 秒，便于 5 分钟视频展示。MENU/UP 等电阻按键在当前硬件上读数不可区分，因此未作为最终入口。

摄像头帧只在开发板内处理。电脑只收到结构化事件，不收到图像。电脑代理限制事件类型；HTTP 模式要求 Bearer Token；锁屏属于显式可选能力，默认关闭。

仓库沉淀了自定义 Skill `skills/develop-agentguard-openvela/`，定义工作区定位、配置门禁、ESP-DL 兼容边界、分区安全、测试和实机证据要求，触发场景为 AgentGuard 的构建、调试、烧录及验收。当前 ESP32-S3 固件采用 ESP-DL 加确定性状态机，没有集成 LLM/ai_agent，因此没有虚构 `/data/agent/skills/` 运行时执行能力；产品运行时“技能”由编译进固件的策略状态机执行。若评审将 `/data/agent/skills/` 解释为所有 AI 硬件作品的强制部署项，这是本版本明确保留的合规差异。

## 3.5 测试、性能与可靠性

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 空场景 | 稳定显示 `FACE:0` | 实机人工验收 |
| 单张正面人脸 | `FACE:1`，显示一个框 | 实机人工验收 |
| 两张人脸 | 基本稳定 `FACE:2`，显示两个框 | 偶发场景波动不作为身份识别 |
| 最终界面 | 摄像头画面正常；底栏显示 `SIT`、`P:ON/OFF`、`D:ON/OFF`，不再显示 `KEY:1331` | 最终固件刷写后实机人工验收 |
| 单帧推理 | 约 840 ms，约 1.19 FPS | 调试期间观测值，随画面/配置变化 |
| 固件大小 | 2,414,732 字节 | 小于 LittleFS 起始边界 `0x300000` |
| 固件空间占用 | 76.76% | 相对于 3,145,728 字节应用区边界 |
| ELF 段大小 | text 1,771,434；data 532,072；bss 961,944 字节 | `xtensa-esp32s3-elf-size` 实测 |
| 最终固件 SHA-256 | `0f37a703e14aa3c38e1281606ac729114f69c3820d2cd74f91e177012139bf2b` | 新模式底栏版本，已刷写且 Flash 哈希验证通过 |
| 板端主机测试 | 全部通过 | `make -C app/agentguard/tests clean test` |
| 电脑端测试 | 22 项通过 | 事件鉴权、USB、日志、今日/累计统计、面板、CSV 和仅本机监听限制 |
| 故障安全 | 电脑断连后板端继续运行 | USB 自动等待并重连 |
| AI Coding 日志 | 16 会话、2,229 事件 | 官方 `validate-log.py` 校验通过 |

当前没有在标准人脸数据集上测量精确率/召回率，因此不提供未经验证的准确率百分比。受提交周期与仪器限制，功耗及两小时以上长时间稳定性尚未量化，作为后续测试项保留。

## 3.6 AI Native 与 AI Coding 数据

AI 用于需求拆解、方案裁剪、ESP-DL/NuttX 适配、编译错误分析、串口/JTAG 诊断、单元测试、交互设计和提交文档整理。关键建议均由编译、测试或开发板实测确认；失败方案（肤色检测、不可区分的电阻按键、当前周期内成本过高的 KWS/主人识别）被及时裁剪。

| 数据项 | 填写内容 |
| --- | --- |
| 使用工具 | Claude Code（MiMo v2.5 Pro）、Codex |
| AI Coding 代码占比 | 约 85%；口径为本项目功能分支新增/修改源码中由 AI 生成或在 AI 建议下修改的行数占比，属于人工估算 |
| 人工主要工作 | 硬件操作、场景准备、实机验收、产品取舍与授权 |
| MCP/外部工具 | 本地终端、Git、USB 串口、JTAG/OpenOCD；未使用第三方业务 MCP |
| 自定义 Skill | `skills/develop-agentguard-openvela/`（开发期） |
| Token/会话数据 | 已归档日志 16 会话、2,229 事件；2,326,350 输入 + 95,461 输出 = 2,421,811 Token |
| 完整日志 | 仓库 `logs/yhx06/`；官方校验器结果为 `ALL OK` |

## 3.7 总结、应用价值与未来计划

AgentGuard 已形成从端侧视觉感知、确定性策略、板端反馈到电脑联动和审计统计的可运行闭环。它适合开放办公室、家庭书房和需要本地隐私保护的工作台，且无需上传视频。

短期可补充长稳与功耗测试、优化两人边缘场景和完善成品外壳。后续可在不改变策略层的前提下替换更高效的 ESP-DL 模型，并在资源允许时增加本地 KWS；身份识别只应在用户明确授权、具备注册/撤销机制并完成隐私评估后考虑。

## 4. 评分维度对应关系

| 评审关注点 | 对应材料/演示 |
| --- | --- |
| 创新性 | 健康提醒与隐私防窥的一体化端侧闭环 |
| 完成度 | `FACE:0/1/2`、双框、提醒、确认、遮罩、日志统计实机演示 |
| 技术性 | ESP-DL 兼容层、状态机、LittleFS、USB 安全代理与故障降级 |
| openvela 使用 | V4L2、LCD、任务、串口、文件系统、NSH |
| AI Native | ESP-DL 本地推理与完整 AI Coding 日志 |
| 可复现性 | 根目录 README、自动化测试、构建和演示说明 |

## 5. 证据与复现入口

源码、测试和 AI Coding 日志位于赛事仓库；根目录 `README.md` 给出构建、烧录、操作和电脑端运行步骤。演示视频按 `docs/submission/演示视频脚本.md` 录制，硬件照片应包含整体连接、空场景、单人框、双人框、久坐提示和隐私遮罩。受提交周期与仪器限制，标准数据集准确率、功耗及两小时以上长稳数据未写入结论，避免用估算替代实测。
